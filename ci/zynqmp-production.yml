header:
  version: 9
  includes:
    - base.yml
    - meta-openembedded.yml

machine: zynqmp-production

local_conf_header:
  bugs: |
    # Only ping until errors can be resolved
    TEST_SUITES = "ping"
