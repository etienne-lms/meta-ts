header:
  version: 9
  includes:
    - base.yml
    - meta-openembedded.yml

machine: tsqemuarm64-secureboot

local_conf_header:
  bugs: |
    # Only ping until errors can be resolved
    TEST_SUITES = "ping"
